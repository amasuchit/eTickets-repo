<tr>
			<td>

				<select id="cinemaDropdown" name="cinemaId" class="form-control">
					<option value="">-- Select Cinema --</option>
			@foreach (var cinema in (SelectList)ViewBag.Cinemas)
			{
								<option value="@cinema.Value">@cinema.Text</option>
			}
				</select>
			</td>
			<td>
				<select id="movieDropdown" name="movieId" class="form-control">
					<option value="">-- Select Movie --</option>
				</select>
			</td>
			 <td>
		<span class="price-field">0.00</span>
	</td>
	<td>
		<button type="button" class="btn btn-danger remove-row"><i class="fa-solid fa-trash"></i> Remove</button>
	</td>
</tr>
		